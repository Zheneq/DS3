set xrange [leftrange:rightrange] #границы графика по X
set yrange [-2:2] #границы графика по Y

unset colorbox
set terminal pngcairo enhanced font "Calibri, 14" size 1366,768
set key off

#set arrow from 50,-5 to 50,5 nohead lc -1;
#set arrow from 100,-5 to 100,5 nohead lc -1;


do for[i=begin/step:end/step] {
	set output sprintf("%s/figures/frame_%06d.png", folder, i);
	plot sprintf("%s/dielcondgfx.txt", folder) index i*step with filledcurves x1 lt rgb "#CCFFCC", \
	sprintf("%s/e%03d.txt", folder, i*step/100) index i*step%100 with lines linewidth 1 lt rgb "red";
}